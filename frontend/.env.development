# Local development environment variables
# Point frontend to local ASP.NET Core backend (per launchSettings.json)
VITE_API_BASE_URL=http://localhost:10000
# Force development environment detection (in case NODE_ENV is accidentally 'production')
VITE_FORCE_DEV=1
